List Varaiable yang harus diatur:

`DB_HOST`: Host atau IP Database Postgresql
`DB_NAME`: Nama database posatgresql
`DB_USER`: Nama user yang digunakan untuk login ke database
`DB_PASSWORD`: Password yang digunakan untuk login ke database
`DB_PORT`: Port database postagresql

`PORT`: Port aplikasi akan dijalankan
